<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#1A1A1A"
    android:padding="16dp">

    <!--
        Phone Controls Layout

        This layout shows on the PHONE only - controls for the HUD.
        The actual HUD content appears on the glasses via HUDPresentation.

        Components:
        - Status bar (connection info)
        - Mode toggle buttons (Text/Camera)
        - Text input (synced to glasses in real-time)
        - Action buttons (Capture, Settings)
    -->

    <!-- Status Bar -->
    <TextView
        android:id="@+id/statusText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Initializing..."
        android:textColor="#00FF00"
        android:textSize="14sp"
        android:fontFamily="monospace"
        android:gravity="center"
        android:padding="12dp"
        android:background="#0A0A0A"
        android:layout_marginBottom="16dp" />

    <!-- Mode Toggle Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="16dp">

        <Button
            android:id="@+id/modeTextButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Text"
            android:textSize="14sp"
            android:backgroundTint="#00AA00"
            android:layout_marginEnd="4dp" />

        <Button
            android:id="@+id/modeCameraButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Camera"
            android:textSize="14sp"
            android:enabled="false"
            android:alpha="0.5"
            android:layout_marginEnd="4dp" />

        <Button
            android:id="@+id/modeClearButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Clear View"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- Text Input Area (visible in text mode) -->
    <EditText
        android:id="@+id/textInput"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#0A0A0A"
        android:gravity="top|start"
        android:hint="@string/text_hint"
        android:padding="12dp"
        android:textColor="#00FF00"
        android:textColorHint="#006600"
        android:textSize="16sp"
        android:inputType="textMultiLine|textNoSuggestions"
        android:fontFamily="monospace"
        android:scrollbars="vertical" />

    <!-- Camera Mode Placeholder (shown when in camera mode) -->
    <LinearLayout
        android:id="@+id/cameraPlaceholder"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center"
        android:background="#0A0A0A"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Camera Preview on Glasses"
            android:textColor="#00AA00"
            android:textSize="20sp"
            android:fontFamily="monospace"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Look at your glasses to see the preview"
            android:textColor="#666666"
            android:textSize="14sp"
            android:fontFamily="monospace" />
    </LinearLayout>

    <!-- Bottom Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="16dp">

        <Button
            android:id="@+id/captureButton"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_weight="2"
            android:text="Save Note"
            android:textSize="18sp"
            android:backgroundTint="#00AA00"
            android:layout_marginEnd="8dp" />

        <Button
            android:id="@+id/settingsButton"
            android:layout_width="0dp"
            android:layout_height="60dp"
            android:layout_weight="1"
            android:text="@string/settings_button"
            android:textSize="14sp" />
    </LinearLayout>

</LinearLayout>
